# تقرير صيانة مشروع الصِّدّيق — 24 سبتمبر 2026

## نطاق الجولة

هذه الجولة مخصصة لملفات React/Vite والواجهة فقط. لم يتم تنفيذ أي تغيير في مخطط Supabase أو RLS أو RPC أو البيانات.

## نتيجة الفحص الثابت النهائي

- ملفات JavaScript/JSX المفحوصة: **191**.
- أخطاء التحليل النحوي: **0**.
- Imports محلية مفقودة: **0**.
- اختلافات حالة الأحرف في مسارات Imports: **0**.
- Assets عامة مستخدمة ومفقودة: **0**.
- أزرار JSX بدون `type`: **0**.
- صور JSX بدون `alt`: **0**.
- روابط `target="_blank"` بدون `rel`: **0**.
- `console.log` / `debugger` / TODO / FIXME / HACK في `src`: **0**.
- أسرار واضحة مثل `service_role` أو مفاتيح سرية hard-coded في الواجهة: **لم يعثر الفحص الثابت على أي منها**.

> هذا الفحص لا يحل محل Build/Lint/اختبار المتصفح الحقيقي، ولا يحل محل مراجعة RLS في Supabase.

## الصيانة المنفذة

### 1. تنظيف المصدر

- إزالة `.env` الحقيقي من نسخة التسليم وإضافة `.env.example` بالأسماء فقط.
- تحديث `.gitignore` لحظر `.env` وجميع نسخه مع السماح بـ `.env.example`.
- إزالة `apply-settings-fix.cjs` بعد التحقق من عدم استيراده أو استخدامه في التشغيل.
- إزالة `src/assets/Z-5.png` لأنه نسخة مطابقة لـ `public/patterns/Z-5.png` وتحديث الاستخدام للمسار العام.
- إزالة imports غير المستخدمة الواضحة التي كشفها lint السابق.
- إزالة دوال/ثوابت ميتة فقط عندما كان حذفها آمنًا وثبت عدم استخدامها وعدم وجود side effects.
- إزالة `console.log` التشخيصية مع الإبقاء على `console.error` و`console.warn` الضرورية للتشخيص.

### 2. إصلاحات React آمنة

- إصلاح تعبير زائد في `SectionCard`.
- إصلاح توليد الدعاء العشوائي في Login بحيث لا تُستدعى `Math.random()` أثناء render.
- نقل تحديثات refs في `HalaqaManagementShared` خارج render إلى Effects.
- تصحيح ترتيب عدد من Effects التي كانت تقرأ دوالًا قبل اكتمال تهيئتها بدون تغيير وظيفة الدوال.
- الحفاظ على التحذيرات التي قد يتطلب إصلاحها تغيير دورة الحياة أو dependencies بدل إصلاحها آليًا بطريقة خطرة.

### 3. Responsive وسلامة النماذج

- الإبقاء على نظام Responsive الموحد وإضافات الجولة السابقة للجوال/التابلت/اللابتوب/الشاشات الكبيرة.
- حماية flex/grid children من توسيع viewport.
- حماية الجداول المعروفة بالـscroll الأفقي عند الحاجة.
- جميع أزرار JSX أصبحت `type="button"` ما لم تكن محددة أصلًا بنوع آخر، لمنع submit غير المقصود داخل النماذج.
- جميع صور JSX لها `alt`.
- تحسين النوافذ والعناصر الحساسة على الشاشات الصغيرة بدون تغيير منطق الصفحات.

### 4. الأداء

- تحويل صفحات الراوتر إلى Route-level lazy loading باستخدام `React.lazy` و`Suspense` بدل تحميل جميع الصفحات في الـbundle الأول.
- استخراج صورتين Base64 كبيرتين من `Login.css` والاستفادة من الملفات المطابقة الموجودة أصلًا في `public/patterns`.
- ضغط PNGs العامة ضغطًا lossless عندما حقق ذلك خفضًا فعليًا للحجم.
- ضغط قالب الشهادة Lossless من قرابة 1.02MB إلى قرابة 0.92MB.
- إضافة Cache لمدة سنة مع `immutable` لمسار Vite الممهور `/assets/*` في Vercel.
- إضافة `preconnect` لمصدر خط Google المستخدم حاليًا.

### 5. حراسة المسارات في الواجهة

> هذه طبقة واجهة فقط، وليست بديلًا عن RLS.

- `/admin/*`: أصبح يتحقق من وجود مستخدم ومن أن الدور `supervisor` أو `admin` قبل إظهار البوابة.
- `/teacher/*`: الحارس الموجود سابقًا للمعلم بقي كما هو.
- `/student/*`: أضيف تمييز صريح لحالة عدم السماح، وإعادة المستخدم غير الطالب/غير المسجل إلى تسجيل الدخول بدل عرض صفحات الطالب.
- أثناء التحقق تظهر حالة تحميل ولا تُعرض الصفحة الداخلية قبل اكتمال فحص الدور.

### 6. رؤوس Vercel الأساسية

الموجود حاليًا:

- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy` لتعطيل camera/microphone/geolocation غير المستخدمة.
- `Strict-Transport-Security`.

لم تتم إضافة CSP صارمة الآن لأن المشروع يحتوي نوافذ طباعة وأنماط/سكربتات inline؛ إضافتها قبل حصر هذه الحالات قد تكسر الطباعة وبعض الواجهات. تتم معالجة CSP في جولة الأمان النهائية.

## نقاط لم يتم تغييرها عمدًا

### Badges / Competitions

`src/pages/Badges.jsx` و`src/pages/Competitions.jsx` ما زالا **متطابقين حرفيًا** ويعرضان منطقًا شبيهًا بإدارة المعلمين بدل واجهة الأوسمة والمسابقات. توجد Services منفصلة للأوسمة والمسابقات في المشروع، لكن بناء واجهتين وظيفيتين جديدتين يعد تطوير ميزة وليس تنظيفًا آمنًا.

لذلك لم يتم اختلاق تصميم أو منطق جديد في جولة الصيانة. **هذه نقطة وظيفية يجب حسمها قبل اعتبار المنتج النهائي مكتملًا.**

### Dependency vulnerability

اختبار المستخدم السابق أبلغ عن ثغرة High واحدة. لم يتم تشغيل `npm audit fix --force` ولم يتم تغيير إصدارات الحزم عشوائيًا. المطلوب تشغيل `npm audit --json` أو `npm run audit:prod` على النسخة الحالية لتحديد الحزمة والمسار والإصلاح المناسب بدقة قبل تغيير أي dependency.

### React hook warnings

الـlint السابق يحتوي عددًا كبيرًا من تحذيرات `exhaustive-deps` و`set-state-in-effect`. تم إصلاح الحالات الآمنة الواضحة فقط. إضافة dependencies أو إعادة هيكلة Effects آليًا قد تغيّر توقيت طلبات البيانات أو تسبب حلقات تحميل، لذلك يلزم تشغيل lint جديد على النسخة الحالية ثم معالجة الباقي حالة بحالة.

## الاختبارات المطلوب تشغيلها على جهاز Windows قبل الجولة التالية

```bash
npm ci
npm run check
npm run audit:prod -- --json
```

أرسل مخرجات `npm run check` و`npm run audit:prod -- --json` كما هي. عندها تتم جولة lint/dependencies الأخيرة على أرقام الأسطر الحالية بدل الاعتماد على تقرير قديم.

## ما بعد إقفال الواجهة

بعد نجاح Build/Lint وحسم Badges/Competitions والتبعيات، ننتقل إلى جولة Supabase والأمان الكامل: RLS، RPC grants، views، direct API access، IDOR/BOLA، Storage، Auth/session، واختبار مصفوفة الأدوار.
